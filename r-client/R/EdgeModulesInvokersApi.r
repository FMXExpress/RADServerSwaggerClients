# EMS API Documentation
#
# Enterprise Mobility Services API      [Learn about EMS](https://www.embarcadero.com/products/rad-studio/enterprise-mobility-services)      EMS (Enterprise Mobility Services) offers a Mobile Enterprise Application Platform (MEAP)      TurnKey Middleware for Interconnected Distributed Apps
#
# OpenAPI spec version: 0.0.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' @title EdgeModulesInvokers operations
#' @description swagger.EdgeModulesInvokers
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#' @field userAgent Set the user agent of the request.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' delete_resource_endpoint Invoke Resource Delete Method
#'
#'
#' delete_resource_endpoint_item Invoke Resource/* Delete Method
#'
#'
#' get_resource_endpoint Invoke Resource Get Method
#'
#'
#' get_resource_endpoint_item Invoke Resource/* Get Method
#'
#'
#' patch_resource_endpoint Invoke Resource Patch Method
#'
#'
#' patch_resource_endpoint_item Invoke Resource/* Patch Method
#'
#'
#' post_resource_endpoint Invoke Resource Post Method
#'
#'
#' post_resource_endpoint_item Invoke Resource/* Post Method
#'
#'
#' put_resource_endpoint Invoke Resource Put Method
#'
#'
#' put_resource_endpoint_item Invoke Resource/* Put Method
#'
#' }
#'
#' @export
EdgeModulesInvokersApi <- R6::R6Class(
  'EdgeModulesInvokersApi',
  public = list(
    userAgent = "Swagger-Codegen/1.0.0/r",
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    delete_resource_endpoint = function(mname, rname, x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      urlPath <- "/edgemodules/{mname}/{rname}"
      if (!missing(`mname`)) {
        urlPath <- gsub(paste0("\\{", "mname", "\\}"), `mname`, urlPath)
      }

      if (!missing(`rname`)) {
        urlPath <- gsub(paste0("\\{", "rname", "\\}"), `rname`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    delete_resource_endpoint_item = function(mname, rname, wildcard, x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      urlPath <- "/edgemodules/{mname}/{rname}/{wildcard}"
      if (!missing(`mname`)) {
        urlPath <- gsub(paste0("\\{", "mname", "\\}"), `mname`, urlPath)
      }

      if (!missing(`rname`)) {
        urlPath <- gsub(paste0("\\{", "rname", "\\}"), `rname`, urlPath)
      }

      if (!missing(`wildcard`)) {
        urlPath <- gsub(paste0("\\{", "wildcard", "\\}"), `wildcard`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_resource_endpoint = function(mname, rname, x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      urlPath <- "/edgemodules/{mname}/{rname}"
      if (!missing(`mname`)) {
        urlPath <- gsub(paste0("\\{", "mname", "\\}"), `mname`, urlPath)
      }

      if (!missing(`rname`)) {
        urlPath <- gsub(paste0("\\{", "rname", "\\}"), `rname`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_resource_endpoint_item = function(mname, rname, wildcard, x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      urlPath <- "/edgemodules/{mname}/{rname}/{wildcard}"
      if (!missing(`mname`)) {
        urlPath <- gsub(paste0("\\{", "mname", "\\}"), `mname`, urlPath)
      }

      if (!missing(`rname`)) {
        urlPath <- gsub(paste0("\\{", "rname", "\\}"), `rname`, urlPath)
      }

      if (!missing(`wildcard`)) {
        urlPath <- gsub(paste0("\\{", "wildcard", "\\}"), `wildcard`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    patch_resource_endpoint = function(mname, rname, body, x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/edgemodules/{mname}/{rname}"
      if (!missing(`mname`)) {
        urlPath <- gsub(paste0("\\{", "mname", "\\}"), `mname`, urlPath)
      }

      if (!missing(`rname`)) {
        urlPath <- gsub(paste0("\\{", "rname", "\\}"), `rname`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PATCH",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    patch_resource_endpoint_item = function(mname, rname, wildcard, body, x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/edgemodules/{mname}/{rname}/{wildcard}"
      if (!missing(`mname`)) {
        urlPath <- gsub(paste0("\\{", "mname", "\\}"), `mname`, urlPath)
      }

      if (!missing(`rname`)) {
        urlPath <- gsub(paste0("\\{", "rname", "\\}"), `rname`, urlPath)
      }

      if (!missing(`wildcard`)) {
        urlPath <- gsub(paste0("\\{", "wildcard", "\\}"), `wildcard`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PATCH",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    post_resource_endpoint = function(mname, rname, body, x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/edgemodules/{mname}/{rname}"
      if (!missing(`mname`)) {
        urlPath <- gsub(paste0("\\{", "mname", "\\}"), `mname`, urlPath)
      }

      if (!missing(`rname`)) {
        urlPath <- gsub(paste0("\\{", "rname", "\\}"), `rname`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    post_resource_endpoint_item = function(mname, rname, wildcard, body, x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/edgemodules/{mname}/{rname}/{wildcard}"
      if (!missing(`mname`)) {
        urlPath <- gsub(paste0("\\{", "mname", "\\}"), `mname`, urlPath)
      }

      if (!missing(`rname`)) {
        urlPath <- gsub(paste0("\\{", "rname", "\\}"), `rname`, urlPath)
      }

      if (!missing(`wildcard`)) {
        urlPath <- gsub(paste0("\\{", "wildcard", "\\}"), `wildcard`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    put_resource_endpoint = function(mname, rname, body, x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/edgemodules/{mname}/{rname}"
      if (!missing(`mname`)) {
        urlPath <- gsub(paste0("\\{", "mname", "\\}"), `mname`, urlPath)
      }

      if (!missing(`rname`)) {
        urlPath <- gsub(paste0("\\{", "rname", "\\}"), `rname`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    put_resource_endpoint_item = function(mname, rname, wildcard, body, x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/edgemodules/{mname}/{rname}/{wildcard}"
      if (!missing(`mname`)) {
        urlPath <- gsub(paste0("\\{", "mname", "\\}"), `mname`, urlPath)
      }

      if (!missing(`rname`)) {
        urlPath <- gsub(paste0("\\{", "rname", "\\}"), `rname`, urlPath)
      }

      if (!missing(`wildcard`)) {
        urlPath <- gsub(paste0("\\{", "wildcard", "\\}"), `wildcard`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
  )
)
