# EMS API Documentation
#
# Enterprise Mobility Services API      [Learn about EMS](https://www.embarcadero.com/products/rad-studio/enterprise-mobility-services)      EMS (Enterprise Mobility Services) offers a Mobile Enterprise Application Platform (MEAP)      TurnKey Middleware for Interconnected Distributed Apps
#
# OpenAPI spec version: 0.0.0
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' @title Users operations
#' @description swagger.Users
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#' @field userAgent Set the user agent of the request.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' add_user Add User
#'
#'
#' delete_user Delete User
#'
#'
#' get_user Get User
#'
#'
#' get_user_fields Get Fields
#'
#'
#' get_user_groups Get User Groups
#'
#'
#' get_users Get Users
#'
#'
#' login_user Log In
#'
#'
#' logout_user sPostLogoutSummaryTitle
#'
#'
#' signup_user Sign Up  User
#'
#'
#' update_user Update User
#'
#' }
#'
#' @export
UsersApi <- R6::R6Class(
  'UsersApi',
  public = list(
    userAgent = "Swagger-Codegen/1.0.0/r",
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    add_user = function(body, x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/users"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- UseridObject$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    delete_user = function(id, x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      urlPath <- "/users/{id}"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "DELETE",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_user = function(id, x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      urlPath <- "/users/{id}"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- UserObject$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_user_fields = function(x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      urlPath <- "/users/fields"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- FieldObject$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_user_groups = function(id, x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      urlPath <- "/users/{id}/groups"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- GroupName$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    get_users = function(x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, skip, limit, order, where, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      if (!missing(`skip`)) {
        queryParams['skip'] <- skip
      }

      if (!missing(`limit`)) {
        queryParams['limit'] <- limit
      }

      if (!missing(`order`)) {
        queryParams['order'] <- order
      }

      if (!missing(`where`)) {
        queryParams['where'] <- where
      }

      urlPath <- "/users"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- UserObject$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    login_user = function(body, x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/users/login"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- UserTokenObject$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    logout_user = function(x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      urlPath <- "/users/logout"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        # void response, no need to return anything
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    signup_user = function(body, x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/users/signup"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "POST",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- UserSignUpResponseObject$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    },
    update_user = function(id, body, x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`x_embarcadero_application_id`)) {
        headerParams['X-Embarcadero-Application-Id'] <- `x_embarcadero_application_id`
      }

      if (!missing(`x_embarcadero_app_secret`)) {
        headerParams['X-Embarcadero-App-Secret'] <- `x_embarcadero_app_secret`
      }

      if (!missing(`x_embarcadero_master_secret`)) {
        headerParams['X-Embarcadero-Master-Secret'] <- `x_embarcadero_master_secret`
      }

      if (!missing(`body`)) {
        body <- `body`$toJSONString()
      } else {
        body <- NULL
      }

      urlPath <- "/users/{id}"
      if (!missing(`id`)) {
        urlPath <- gsub(paste0("\\{", "id", "\\}"), `id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "PUT",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- UpdatedObject$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
  )
)
