--[[
  EMS API Documentation
 
  Enterprise Mobility Services API      [Learn about EMS](https://www.embarcadero.com/products/rad-studio/enterprise-mobility-services)      EMS (Enterprise Mobility Services) offers a Mobile Enterprise Application Platform (MEAP)      TurnKey Middleware for Interconnected Distributed Apps
 
  OpenAPI spec version: 0.0.0
  
  Generated by: https://github.com/swagger-api/swagger-codegen.git
]]

--package swagger-client

local http_request = require "http.request"
local http_util = require "http.util"
local dkjson = require "dkjson"
local basexx = require "basexx"

-- model import

local api_doc_api = {}
local api_doc_api_mt = {
	__name = "api_doc_api";
	__index = api_doc_api;
}

local function new_api_doc_api(host, basePath, schemes)
	local schemes_map = {}
	for _,v in ipairs(schemes) do
		schemes_map[v] = v
	end
	local default_scheme = schemes_map.https or schemes_map.http
	return setmetatable({
		host = host;
		basePath = basePath or "http://localhost:8080";
		schemes = schemes_map;
		default_scheme = default_scheme;
		http_username = nil;
		http_password = nil;
		api_key = {};
		access_token = nil;
	}, api_doc_api_mt)
end

function api_doc_api:a_pi(x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		path = string.format("%s/api",
			self.basePath);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	-- ref: https://github.com/swagger-api/swagger-codegen/pull/6252#issuecomment-321199879
	--local var_accept = { "application/json" }
	req.headers:upsert("content-type", "application/json")

	if x_embarcadero_application_id then
		req.headers:upsert("X-Embarcadero-Application-Id", x_embarcadero_application_id)
	end
	if x_embarcadero_app_secret then
		req.headers:upsert("X-Embarcadero-App-Secret", x_embarcadero_app_secret)
	end
	if x_embarcadero_master_secret then
		req.headers:upsert("X-Embarcadero-Master-Secret", x_embarcadero_master_secret)
	end

	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		return nil, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function api_doc_api:get_apijson_format(x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		path = string.format("%s/api/apidoc.json",
			self.basePath);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	if x_embarcadero_application_id then
		req.headers:upsert("X-Embarcadero-Application-Id", x_embarcadero_application_id)
	end
	if x_embarcadero_app_secret then
		req.headers:upsert("X-Embarcadero-App-Secret", x_embarcadero_app_secret)
	end
	if x_embarcadero_master_secret then
		req.headers:upsert("X-Embarcadero-Master-Secret", x_embarcadero_master_secret)
	end

	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		return nil, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function api_doc_api:get_apiyaml_format(x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		path = string.format("%s/api/apidoc.yaml",
			self.basePath);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	if x_embarcadero_application_id then
		req.headers:upsert("X-Embarcadero-Application-Id", x_embarcadero_application_id)
	end
	if x_embarcadero_app_secret then
		req.headers:upsert("X-Embarcadero-App-Secret", x_embarcadero_app_secret)
	end
	if x_embarcadero_master_secret then
		req.headers:upsert("X-Embarcadero-Master-Secret", x_embarcadero_master_secret)
	end

	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		return nil, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

function api_doc_api:get_apiyaml_format_end_point(item, x_embarcadero_application_id, x_embarcadero_app_secret, x_embarcadero_master_secret)
	local req = http_request.new_from_uri({
		scheme = self.default_scheme;
		host = self.host;
		path = string.format("%s/api/%s/apidoc.yaml",
			self.basePath, item);
	})

	-- set HTTP verb
	req.headers:upsert(":method", "GET")
	-- TODO: create a function to select proper content-type
	-- ref: https://github.com/swagger-api/swagger-codegen/pull/6252#issuecomment-321199879
	--local var_accept = { "application/json" }
	req.headers:upsert("content-type", "application/json")

	if x_embarcadero_application_id then
		req.headers:upsert("X-Embarcadero-Application-Id", x_embarcadero_application_id)
	end
	if x_embarcadero_app_secret then
		req.headers:upsert("X-Embarcadero-App-Secret", x_embarcadero_app_secret)
	end
	if x_embarcadero_master_secret then
		req.headers:upsert("X-Embarcadero-Master-Secret", x_embarcadero_master_secret)
	end

	-- make the HTTP call
	local headers, stream, errno = req:go()
	if not headers then
		return nil, stream, errno
	end
	local http_status = headers:get(":status")
	if http_status:sub(1,1) == "2" then
		return nil, headers
	else
		local body, err, errno2 = stream:get_body_as_string()
		if not body then
			return nil, err, errno2
		end
		stream:shutdown()
		-- return the error message (http body)
		return nil, http_status, body
	end
end

return {
	new = new_api_doc_api;
}

