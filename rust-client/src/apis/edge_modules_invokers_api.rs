/* 
 * EMS API Documentation
 *
 * Enterprise Mobility Services API      [Learn about EMS](https://www.embarcadero.com/products/rad-studio/enterprise-mobility-services)      EMS (Enterprise Mobility Services) offers a Mobile Enterprise Application Platform (MEAP)      TurnKey Middleware for Interconnected Distributed Apps
 *
 * OpenAPI spec version: 0.0.0
 * 
 * Generated by: https://github.com/swagger-api/swagger-codegen.git
 */

use std::rc::Rc;
use std::borrow::Borrow;
use std::borrow::Cow;
use std::collections::HashMap;

use hyper;
use serde_json;
use futures;
use futures::{Future, Stream};

use hyper::header::UserAgent;

use super::{Error, configuration};

pub struct EdgeModulesInvokersApiClient<C: hyper::client::Connect> {
    configuration: Rc<configuration::Configuration<C>>,
}

impl<C: hyper::client::Connect> EdgeModulesInvokersApiClient<C> {
    pub fn new(configuration: Rc<configuration::Configuration<C>>) -> EdgeModulesInvokersApiClient<C> {
        EdgeModulesInvokersApiClient {
            configuration: configuration,
        }
    }
}

pub trait EdgeModulesInvokersApi {
    fn delete_resource_endpoint(&self, mname: &str, rname: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>>;
    fn delete_resource_endpoint_item(&self, mname: &str, rname: &str, wildcard: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>>;
    fn get_resource_endpoint(&self, mname: &str, rname: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>>;
    fn get_resource_endpoint_item(&self, mname: &str, rname: &str, wildcard: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>>;
    fn patch_resource_endpoint(&self, mname: &str, rname: &str, body: ::models::Value, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>>;
    fn patch_resource_endpoint_item(&self, mname: &str, rname: &str, wildcard: &str, body: ::models::Value, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>>;
    fn post_resource_endpoint(&self, mname: &str, rname: &str, body: ::models::Value, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>>;
    fn post_resource_endpoint_item(&self, mname: &str, rname: &str, wildcard: &str, body: ::models::Value, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>>;
    fn put_resource_endpoint(&self, mname: &str, rname: &str, body: ::models::Value, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>>;
    fn put_resource_endpoint_item(&self, mname: &str, rname: &str, wildcard: &str, body: ::models::Value, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>>;
}


impl<C: hyper::client::Connect>EdgeModulesInvokersApi for EdgeModulesInvokersApiClient<C> {
    fn delete_resource_endpoint(&self, mname: &str, rname: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Delete;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/{mname}/{rname}?{}", configuration.base_path, query_string, mname=mname, rname=rname);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }



        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|_| futures::future::ok(()))
        )
    }

    fn delete_resource_endpoint_item(&self, mname: &str, rname: &str, wildcard: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Delete;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/{mname}/{rname}/{wildcard}?{}", configuration.base_path, query_string, mname=mname, rname=rname, wildcard=wildcard);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }



        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|_| futures::future::ok(()))
        )
    }

    fn get_resource_endpoint(&self, mname: &str, rname: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/{mname}/{rname}?{}", configuration.base_path, query_string, mname=mname, rname=rname);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }



        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|_| futures::future::ok(()))
        )
    }

    fn get_resource_endpoint_item(&self, mname: &str, rname: &str, wildcard: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/{mname}/{rname}/{wildcard}?{}", configuration.base_path, query_string, mname=mname, rname=rname, wildcard=wildcard);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }



        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|_| futures::future::ok(()))
        )
    }

    fn patch_resource_endpoint(&self, mname: &str, rname: &str, body: ::models::Value, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Patch;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/{mname}/{rname}?{}", configuration.base_path, query_string, mname=mname, rname=rname);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }


        let serialized = serde_json::to_string(&body).unwrap();
        req.headers_mut().set(hyper::header::ContentType::json());
        req.headers_mut().set(hyper::header::ContentLength(serialized.len() as u64));
        req.set_body(serialized);

        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|_| futures::future::ok(()))
        )
    }

    fn patch_resource_endpoint_item(&self, mname: &str, rname: &str, wildcard: &str, body: ::models::Value, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Patch;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/{mname}/{rname}/{wildcard}?{}", configuration.base_path, query_string, mname=mname, rname=rname, wildcard=wildcard);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }


        let serialized = serde_json::to_string(&body).unwrap();
        req.headers_mut().set(hyper::header::ContentType::json());
        req.headers_mut().set(hyper::header::ContentLength(serialized.len() as u64));
        req.set_body(serialized);

        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|_| futures::future::ok(()))
        )
    }

    fn post_resource_endpoint(&self, mname: &str, rname: &str, body: ::models::Value, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Post;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/{mname}/{rname}?{}", configuration.base_path, query_string, mname=mname, rname=rname);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }


        let serialized = serde_json::to_string(&body).unwrap();
        req.headers_mut().set(hyper::header::ContentType::json());
        req.headers_mut().set(hyper::header::ContentLength(serialized.len() as u64));
        req.set_body(serialized);

        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|_| futures::future::ok(()))
        )
    }

    fn post_resource_endpoint_item(&self, mname: &str, rname: &str, wildcard: &str, body: ::models::Value, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Post;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/{mname}/{rname}/{wildcard}?{}", configuration.base_path, query_string, mname=mname, rname=rname, wildcard=wildcard);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }


        let serialized = serde_json::to_string(&body).unwrap();
        req.headers_mut().set(hyper::header::ContentType::json());
        req.headers_mut().set(hyper::header::ContentLength(serialized.len() as u64));
        req.set_body(serialized);

        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|_| futures::future::ok(()))
        )
    }

    fn put_resource_endpoint(&self, mname: &str, rname: &str, body: ::models::Value, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Put;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/{mname}/{rname}?{}", configuration.base_path, query_string, mname=mname, rname=rname);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }


        let serialized = serde_json::to_string(&body).unwrap();
        req.headers_mut().set(hyper::header::ContentType::json());
        req.headers_mut().set(hyper::header::ContentLength(serialized.len() as u64));
        req.set_body(serialized);

        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|_| futures::future::ok(()))
        )
    }

    fn put_resource_endpoint_item(&self, mname: &str, rname: &str, wildcard: &str, body: ::models::Value, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Put;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/{mname}/{rname}/{wildcard}?{}", configuration.base_path, query_string, mname=mname, rname=rname, wildcard=wildcard);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }


        let serialized = serde_json::to_string(&body).unwrap();
        req.headers_mut().set(hyper::header::ContentType::json());
        req.headers_mut().set(hyper::header::ContentLength(serialized.len() as u64));
        req.set_body(serialized);

        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|_| futures::future::ok(()))
        )
    }

}
