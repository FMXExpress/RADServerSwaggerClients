/* 
 * EMS API Documentation
 *
 * Enterprise Mobility Services API      [Learn about EMS](https://www.embarcadero.com/products/rad-studio/enterprise-mobility-services)      EMS (Enterprise Mobility Services) offers a Mobile Enterprise Application Platform (MEAP)      TurnKey Middleware for Interconnected Distributed Apps
 *
 * OpenAPI spec version: 0.0.0
 * 
 * Generated by: https://github.com/swagger-api/swagger-codegen.git
 */

use std::rc::Rc;
use std::borrow::Borrow;
use std::borrow::Cow;
use std::collections::HashMap;

use hyper;
use serde_json;
use futures;
use futures::{Future, Stream};

use hyper::header::UserAgent;

use super::{Error, configuration};

pub struct EdgeModulesApiClient<C: hyper::client::Connect> {
    configuration: Rc<configuration::Configuration<C>>,
}

impl<C: hyper::client::Connect> EdgeModulesApiClient<C> {
    pub fn new(configuration: Rc<configuration::Configuration<C>>) -> EdgeModulesApiClient<C> {
        EdgeModulesApiClient {
            configuration: configuration,
        }
    }
}

pub trait EdgeModulesApi {
    fn get_module(&self, mname: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::EdgeModuleObject, Error = Error<serde_json::Value>>>;
    fn get_module_resource(&self, mname: &str, name: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::EdgeModuleResourceObject, Error = Error<serde_json::Value>>>;
    fn get_module_resources(&self, mname: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = Vec<::models::EdgeModuleResourceObject>, Error = Error<serde_json::Value>>>;
    fn get_modules(&self, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = Vec<::models::EdgeModuleObject>, Error = Error<serde_json::Value>>>;
    fn get_modules_fields(&self, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::FieldsEdgeModuleObject, Error = Error<serde_json::Value>>>;
    fn get_resources(&self, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = Vec<::models::EdgeModuleResourceObject>, Error = Error<serde_json::Value>>>;
    fn get_resources_fields(&self, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::FieldsEdgeModuleObject, Error = Error<serde_json::Value>>>;
    fn register_module(&self, body: ::models::EdgeModuleAddObject, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::EdgeModuleAddedObject, Error = Error<serde_json::Value>>>;
    fn register_module_resource(&self, mname: &str, body: ::models::EdgeModuleResourceAddObject, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::EdgeModuleResourceAddedObject, Error = Error<serde_json::Value>>>;
    fn unregister_module(&self, mname: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>>;
    fn unregister_module_resource(&self, mname: &str, name: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>>;
    fn update_module(&self, mname: &str, body: ::models::EdgeModuleUpdateObject, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::EdgeModuleUpdatedObject, Error = Error<serde_json::Value>>>;
    fn update_module_resource(&self, mname: &str, name: &str, body: ::models::EdgeModuleResourceUpdateObject, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::EdgeModuleResourceUpdatedObject, Error = Error<serde_json::Value>>>;
}


impl<C: hyper::client::Connect>EdgeModulesApi for EdgeModulesApiClient<C> {
    fn get_module(&self, mname: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::EdgeModuleObject, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/{mname}?{}", configuration.base_path, query_string, mname=mname);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }



        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::EdgeModuleObject, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn get_module_resource(&self, mname: &str, name: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::EdgeModuleResourceObject, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/{mname}/resources/{name}?{}", configuration.base_path, query_string, mname=mname, name=name);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }



        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::EdgeModuleResourceObject, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn get_module_resources(&self, mname: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = Vec<::models::EdgeModuleResourceObject>, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/{mname}/resources?{}", configuration.base_path, query_string, mname=mname);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }



        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<Vec<::models::EdgeModuleResourceObject>, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn get_modules(&self, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = Vec<::models::EdgeModuleObject>, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules?{}", configuration.base_path, query_string);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }



        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<Vec<::models::EdgeModuleObject>, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn get_modules_fields(&self, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::FieldsEdgeModuleObject, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/fields?{}", configuration.base_path, query_string);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }



        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::FieldsEdgeModuleObject, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn get_resources(&self, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = Vec<::models::EdgeModuleResourceObject>, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/resources?{}", configuration.base_path, query_string);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }



        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<Vec<::models::EdgeModuleResourceObject>, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn get_resources_fields(&self, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::FieldsEdgeModuleObject, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/resources/fields?{}", configuration.base_path, query_string);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }



        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::FieldsEdgeModuleObject, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn register_module(&self, body: ::models::EdgeModuleAddObject, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::EdgeModuleAddedObject, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Post;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules?{}", configuration.base_path, query_string);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }


        let serialized = serde_json::to_string(&body).unwrap();
        req.headers_mut().set(hyper::header::ContentType::json());
        req.headers_mut().set(hyper::header::ContentLength(serialized.len() as u64));
        req.set_body(serialized);

        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::EdgeModuleAddedObject, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn register_module_resource(&self, mname: &str, body: ::models::EdgeModuleResourceAddObject, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::EdgeModuleResourceAddedObject, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Post;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/{mname}/resources?{}", configuration.base_path, query_string, mname=mname);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }


        let serialized = serde_json::to_string(&body).unwrap();
        req.headers_mut().set(hyper::header::ContentType::json());
        req.headers_mut().set(hyper::header::ContentLength(serialized.len() as u64));
        req.set_body(serialized);

        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::EdgeModuleResourceAddedObject, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn unregister_module(&self, mname: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Delete;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/{mname}?{}", configuration.base_path, query_string, mname=mname);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }



        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|_| futures::future::ok(()))
        )
    }

    fn unregister_module_resource(&self, mname: &str, name: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Delete;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/{mname}/resources/{name}?{}", configuration.base_path, query_string, mname=mname, name=name);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }



        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|_| futures::future::ok(()))
        )
    }

    fn update_module(&self, mname: &str, body: ::models::EdgeModuleUpdateObject, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::EdgeModuleUpdatedObject, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Put;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/{mname}?{}", configuration.base_path, query_string, mname=mname);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }


        let serialized = serde_json::to_string(&body).unwrap();
        req.headers_mut().set(hyper::header::ContentType::json());
        req.headers_mut().set(hyper::header::ContentLength(serialized.len() as u64));
        req.set_body(serialized);

        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::EdgeModuleUpdatedObject, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn update_module_resource(&self, mname: &str, name: &str, body: ::models::EdgeModuleResourceUpdateObject, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::EdgeModuleResourceUpdatedObject, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Put;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/edgemodules/{mname}/resources/{name}?{}", configuration.base_path, query_string, mname=mname, name=name);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }


        let serialized = serde_json::to_string(&body).unwrap();
        req.headers_mut().set(hyper::header::ContentType::json());
        req.headers_mut().set(hyper::header::ContentLength(serialized.len() as u64));
        req.set_body(serialized);

        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::EdgeModuleResourceUpdatedObject, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

}
