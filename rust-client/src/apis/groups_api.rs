/* 
 * EMS API Documentation
 *
 * Enterprise Mobility Services API      [Learn about EMS](https://www.embarcadero.com/products/rad-studio/enterprise-mobility-services)      EMS (Enterprise Mobility Services) offers a Mobile Enterprise Application Platform (MEAP)      TurnKey Middleware for Interconnected Distributed Apps
 *
 * OpenAPI spec version: 0.0.0
 * 
 * Generated by: https://github.com/swagger-api/swagger-codegen.git
 */

use std::rc::Rc;
use std::borrow::Borrow;
use std::borrow::Cow;
use std::collections::HashMap;

use hyper;
use serde_json;
use futures;
use futures::{Future, Stream};

use hyper::header::UserAgent;

use super::{Error, configuration};

pub struct GroupsApiClient<C: hyper::client::Connect> {
    configuration: Rc<configuration::Configuration<C>>,
}

impl<C: hyper::client::Connect> GroupsApiClient<C> {
    pub fn new(configuration: Rc<configuration::Configuration<C>>) -> GroupsApiClient<C> {
        GroupsApiClient {
            configuration: configuration,
        }
    }
}

pub trait GroupsApi {
    fn add_group(&self, body: ::models::GroupAddObject, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>>;
    fn delete_group(&self, item: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>>;
    fn get_group(&self, item: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::GroupObject, Error = Error<serde_json::Value>>>;
    fn get_group_fields(&self, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = Vec<::models::FieldGroupObject>, Error = Error<serde_json::Value>>>;
    fn get_groups(&self, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str, skip: f32, limit: f32, order: f32, _where: &str) -> Box<Future<Item = Vec<::models::GroupObject>, Error = Error<serde_json::Value>>>;
    fn update_group(&self, item: &str, body: ::models::UpdateGroupObject, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::UpdatedGroupObject, Error = Error<serde_json::Value>>>;
}


impl<C: hyper::client::Connect>GroupsApi for GroupsApiClient<C> {
    fn add_group(&self, body: ::models::GroupAddObject, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Post;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/groups?{}", configuration.base_path, query_string);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }


        let serialized = serde_json::to_string(&body).unwrap();
        req.headers_mut().set(hyper::header::ContentType::json());
        req.headers_mut().set(hyper::header::ContentLength(serialized.len() as u64));
        req.set_body(serialized);

        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|_| futures::future::ok(()))
        )
    }

    fn delete_group(&self, item: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = (), Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Delete;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/groups/{item}?{}", configuration.base_path, query_string, item=item);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }



        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|_| futures::future::ok(()))
        )
    }

    fn get_group(&self, item: &str, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::GroupObject, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/groups/{item}?{}", configuration.base_path, query_string, item=item);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }



        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::GroupObject, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn get_group_fields(&self, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = Vec<::models::FieldGroupObject>, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/groups/fields?{}", configuration.base_path, query_string);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }



        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<Vec<::models::FieldGroupObject>, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn get_groups(&self, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str, skip: f32, limit: f32, order: f32, _where: &str) -> Box<Future<Item = Vec<::models::GroupObject>, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Get;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.append_pair("skip", &skip.to_string());
            query.append_pair("limit", &limit.to_string());
            query.append_pair("order", &order.to_string());
            query.append_pair("where", &_where.to_string());
            query.finish()
        };
        let uri_str = format!("{}/groups?{}", configuration.base_path, query_string);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }



        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<Vec<::models::GroupObject>, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

    fn update_group(&self, item: &str, body: ::models::UpdateGroupObject, x_embarcadero_application_id: &str, x_embarcadero_app_secret: &str, x_embarcadero_master_secret: &str) -> Box<Future<Item = ::models::UpdatedGroupObject, Error = Error<serde_json::Value>>> {
        let configuration: &configuration::Configuration<C> = self.configuration.borrow();

        let method = hyper::Method::Put;

        let query_string = {
            let mut query = ::url::form_urlencoded::Serializer::new(String::new());
            query.finish()
        };
        let uri_str = format!("{}/groups/{item}?{}", configuration.base_path, query_string, item=item);

        // TODO(farcaller): handle error
        // if let Err(e) = uri {
        //     return Box::new(futures::future::err(e));
        // }
        let mut uri: hyper::Uri = uri_str.parse().unwrap();

        let mut req = hyper::Request::new(method, uri);

        if let Some(ref user_agent) = configuration.user_agent {
            req.headers_mut().set(UserAgent::new(Cow::Owned(user_agent.clone())));
        }

        {
            let mut headers = req.headers_mut();
            headers.set_raw("X-Embarcadero-Application-Id", x_embarcadero_application_id);
            headers.set_raw("X-Embarcadero-App-Secret", x_embarcadero_app_secret);
            headers.set_raw("X-Embarcadero-Master-Secret", x_embarcadero_master_secret);
        }


        let serialized = serde_json::to_string(&body).unwrap();
        req.headers_mut().set(hyper::header::ContentType::json());
        req.headers_mut().set(hyper::header::ContentLength(serialized.len() as u64));
        req.set_body(serialized);

        // send request
        Box::new(
        configuration.client.request(req)
            .map_err(|e| Error::from(e))
            .and_then(|resp| {
                let status = resp.status();
                resp.body().concat2()
                    .and_then(move |body| Ok((status, body)))
                    .map_err(|e| Error::from(e))
            })
            .and_then(|(status, body)| {
                if status.is_success() {
                    Ok(body)
                } else {
                    Err(Error::from((status, &*body)))
                }
            })
            .and_then(|body| {
                let parsed: Result<::models::UpdatedGroupObject, _> = serde_json::from_slice(&body);
                parsed.map_err(|e| Error::from(e))
            })
        )
    }

}
